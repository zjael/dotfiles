monitor=,preferred,auto,1
monitor=HDMI-A-1,1920x1080@144,auto,1

exec-once=$HOME/.config/hypr/autostart

workspace=HDMI-A-1,1

input {
    kb_layout=dk
    follow_mouse=1
    force_no_accel=true
    touchpad {
        natural_scroll=no
    }
}

gestures {
    workspace_swipe=yes
    workspace_swipe_fingers=3
    workspace_swipe_distance=100
    workspace_swipe_invert=false
    workspace_swipe_forever=true
}

misc{
}

binds {
    allow_workspace_cycles=true
}

general {
    sensitivity=0.8 # for mouse cursor

    gaps_in=2
    gaps_out=4
    border_size=1
    no_border_on_floating = true
    col.active_border=0xffa6e3a1 0xffa6adc8 0xffeba0ac 10deg
    #col.active_border=rgba(cba6f7ff) rgba(89b4faff) rgba(94e2d5ff) 10deg
    col.inactive_border=0xff45475a
    apply_sens_to_raw=0 # whether to apply the sensitivity to raw input (e.g. used by games where you aim using your mouse)
}

decoration {
    blur_new_optimizations = true
    drop_shadow = true
    shadow_range=100
    shadow_render_power=5
    col.shadow= 0x33000000
    col.shadow_inactive=0x22000000
    rounding=2
    blur=0
    blur_size=1 # minimum 1
    blur_passes=1 # minimum 1, more passes = more resource intensive.
    # Your blur "amount" is blur_size * blur_passes, but high blur_size (over around 5-ish) will produce artifacts.
    # if you want heavy blur, you need to up the blur_passes.
    # the more passes, the more you can up the blur_size without noticing artifacts.
}



animations {
    enabled=1
    bezier=overshot,0.05,0.99,0.30,1.1
    #bezier=overshot,0.13,0.99,0.29,1.1
    animation=windows,1,4,overshot,slide
    animation=border,1,10,default
    animation=fade,1,10,default
    animation=workspaces,1,6,overshot,slidevert
}

dwindle {
    pseudotile=1 # enable pseudotiling on dwindle
    preserve_split = true
    force_split=0
    col.group_border=0xff89dceb
    col.group_border_active=0xfff9e2af
}

master{

}

# Window Rules
windowrule=move center,title:^(floating_kitty)$
windowrule=size 800 500,title:^(floating_kitty)$
windowrule=float,title:^(floating_kitty)$

windowrule=animation slide,title:^(full_kitty)$
windowrule=float,title:^(full_kitty)$
windowrule=tile,title:^(kitty)$
windowrule=workspace 2,title:^(Firefox)

windowrule=move center,title:^(Bitwarden)$
windowrule=size 1200 700,title:^(Bitwarden)$
windowrule=float,title:^(Bitwarden)$
windowrule=forceinput,title:^(Bitwarden)$

# Keybinds
bindm=SUPER,mouse:272,movewindow
bindm=SUPER,mouse:273,resizewindow

bind=SUPER,t,exec,kitty --start-as=fullscreen -o 'font_size=14' --title full_kitty
bind=SUPER,RETURN,exec,kitty --title floating_kitty
bind=ALT,RETURN,exec,kitty --single-instance
bind=SUPER,½,exec,kitty --single-instance

bind=SUPER,Q,killactive,
bind=SUPER SHIFT,Q,exit,
bind=SUPER,F,fullscreen,
bind=SUPER,Space,togglefloating,
bind=SUPER,S,togglesplit,
bind=SUPER,P,pseudo,
bind=SUPER,g,togglegroup
bind=SUPER,tab,changegroupactive
bind=SUPER,S,togglespecialworkspace,spotify
bind=SUPER,x,togglespecialworkspace,scratchpad

bind=SUPER,E,exec,nautilus
bind=SUPER,R,exec,wofi --show drun -o DP-3
bind=SUPER,L,exec,~/.config/hypr/scripts/lock
bind=,Print,exec,~/.config/hypr/scripts/screenshot
bind=SUPER,B,exec, killall -3 eww & sleep 1 && ~/.config/eww/launch_bar.sh
bind=SUPER,W,exec,~/.config/eww/scripts/wallpaper_picker

bind=,XF86AudioRaiseVolume,exec, pamixer -i 5
bind=,XF86AudioLowerVolume,exec, pamixer -d 5
bind=,XF86AudioMute,exec, pamixer -t
bind=,XF86AudioMicMute,exec, pamixer --default-source -t
bind=,XF86MonBrightnessUp,exec, pkbrillo -A 5
bind=,XF86MonBrightnessDown, exec, brillo -U 5
bind=,XF86AudioPlay,exec, playerctl play-pause
bind=,XF86AudioNext,exec, playerctl next
bind=,XF86AudioPrev,exec, playerctl previous

bind=SUPER,1,workspace,1
bind=SUPER,2,workspace,2
bind=SUPER,3,workspace,3
bind=SUPER,4,workspace,4
bind=SUPER,5,workspace,5
bind=SUPER,6,workspace,6
bind=SUPER,7,workspace,7
bind=SUPER,8,workspace,8
bind=SUPER,9,workspace,9
bind=SUPER,0,workspace,10
bind=ALT,tab,workspace,previous

bind=CTRL,1,movetoworkspace,1
bind=CTRL,2,movetoworkspace,2
bind=CTRL,3,movetoworkspace,3
bind=CTRL,4,movetoworkspace,4
bind=CTRL,5,movetoworkspace,5
bind=CTRL,6,movetoworkspace,6
bind=CTRL,7,movetoworkspace,7
bind=CTRL,8,movetoworkspace,8
bind=CTRL,9,movetoworkspace,9
bind=CTRL,0,movetoworkspace,10

bind=SUPER CTRL,left,resizeactive,-100 0
bind=SUPER CTRL,right,resizeactive,100 0
bind=SUPER CTRL,up,resizeactive,0 -100
bind=SUPER CTRL,down,resizeactive,0 100

bind=SUPER,left,movefocus,l
bind=SUPER,right,movefocus,r
bind=SUPER,up,movefocus,u
bind=SUPER,down,movefocus,d
bind=SUPER SHIFT,left,movewindow,l
bind=SUPER SHIFT,right,movewindow,r
bind=SUPER SHIFT,up,movewindow,u
bind=SUPER SHIFT,down,movewindow,d
bind=SUPER,mouse_down,workspace,e+1
bind=SUPER,mouse_up,workspace,e-1

# Auto start
exec=killall -3 eww & sleep 1 && ~/.config/eww/launch.sh
exec-once=[workspace 2 silent] firefox
exec-once=[workspace 3 silent] kitty -o 'font_size=14' --title full_kitty
exec-once=[workspace 4 silent] spotify
exec-once=[workspace special:scratchpad silent] kitty --title scratchpad
#exec-once=[workspace special:spotify silent] spotify
exec-once=bitwarden-desktop